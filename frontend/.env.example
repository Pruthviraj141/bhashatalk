# Leave empty to use same-origin requests through Vite proxy (/api and /ws).
# This is recommended for local + ngrok usage.
VITE_WS_HOST=

# Optional absolute backend URL (e.g., https://api.example.com).
# Keep empty for local dev proxy mode.
VITE_API_BASE_URL=
